{
  "//": "Pyright/Pylance can't follow PEP 660 editable installs of PEP 420 namespace packages. Point it at each member's source root (the dir that contains `petbot/`) so `petbot.*` imports resolve in the editor. CI's typing authority remains `mypy --strict`.",
  "venvPath": ".",
  "venv": ".venv",
  "extraPaths": [
    "domain",
    "types",
    "platform",
    "skills/math",
    "skills/booru",
    "skills/music",
    "skills/chat",
    "frontends/discord",
    "workers/brain",
    "workers/music"
  ],
  "executionEnvironments": [
    { "root": "." }
  ]
}
